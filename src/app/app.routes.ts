import { Routes } from '@angular/router';
import { ValidarPlacaComponent } from './validar-placa/validar-placa.component';

export const routes: Routes = [
    {
        path: '',
        component: ValidarPlacaComponent
    }
];
